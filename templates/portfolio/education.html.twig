{% extends 'base.html.twig' %}

{% block body %}
  <section class="section">
    <div class="container-shell">
      <div class="section-subtitle">{{ 'nav.education'|trans }}</div>
      <h1 class="section-title">{{ 'section.education'|trans }}</h1>

      <div class="mt-8 grid gap-6">
        {% for education in educations %}
          <div class="card" data-reveal>
            <div class="flex flex-col gap-2 md:flex-row md:items-start md:justify-between">
              <div>
                <h3 class="text-xl font-semibold text-ink">{{ localized(education, 'degree', app.request.locale) }}</h3>
                <div class="text-sm text-muted">{{ localized(education, 'institution', app.request.locale) }}</div>
                {% if localized(education, 'location', app.request.locale) %}
                  <div class="text-xs uppercase tracking-[0.2em] text-slate-400">{{ localized(education, 'location', app.request.locale) }}</div>
                {% endif %}
              </div>
              <div class="text-sm text-slate-500">
                {% set start = education.startDate|format_datetime(locale=app.request.locale, pattern='MMM yyyy') %}
                {% if education.isCurrent %}
                  {{ start }} — {{ 'ui.present'|trans }}
                {% elseif education.endDate %}
                  {% set end = education.endDate|format_datetime(locale=app.request.locale, pattern='MMM yyyy') %}
                  {{ start }} — {{ end }}
                {% else %}
                  {{ start }}
                {% endif %}
              </div>
            </div>
            <p class="mt-4 text-sm text-muted">{{ localized(education, 'description', app.request.locale) }}</p>
          </div>
        {% else %}
          <div class="card" data-reveal>{{ 'ui.empty_education'|trans }}</div>
        {% endfor %}
      </div>
    </div>
  </section>
{% endblock %}
